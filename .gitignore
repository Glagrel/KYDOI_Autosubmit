# Runtime directories — contain patient data and local files
archive/
incoming/
logs/

# Built artifacts — generated by NSIS, not source
KY_DOI_Installer.exe
Uninstall.exe

# Local runtime config — TESTING_MODE etc. vary per machine
# settings.json is tracked as a defaults template; untrack your local copy with:
#   git update-index --skip-worktree settings.json

# Claude Code editor config
.claude/

# Python cache
__pycache__/
*.pyc
*.pyo

# Windows
Thumbs.db
desktop.ini
